{
  "name": "landpage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean_cache": "rm -rf .next",
    "dev": "npm run clean_cache && next dev",
    "build": "npm run clean_cache && next build",
    "start": "npm run clean_cache &&  next start",
    "lint": "next lint",
    "export": "next export"
  },
  "dependencies": {
    "next": "14.2.22",
    "react": "^18",
    "react-dom": "^18",
    "react-icons": "^5.4.0"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
